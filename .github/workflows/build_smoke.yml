name: "Build & Smoke Test"

on:
  pull_request:
    types: [opened, reopened, synchronize]
  workflow_dispatch:

jobs:
  build-smoke:
    name: "Build & Smoke (${{ matrix.os }})"
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Setup Poetry
        uses: Gr1N/setup-poetry@v9

      - name: Build viewer + install
        # Force bash so windows-latest matches macOS/Linux (default windows
        # shell is pwsh, which has different escaping and operator semantics).
        shell: bash
        env:
          NODE_OPTIONS: --max-old-space-size=4096
        run: |
          cd viewer
          yarn install --network-timeout 600000 && yarn deploy
          cd ..
          poetry install
          poetry run write-schema-json

      - name: Build Artifact
        shell: bash
        run: poetry run build

      - name: Smoke test built binary
        shell: bash
        run: bash scripts/smoke_test_binary.sh dist/visivo test-projects/docs-examples
