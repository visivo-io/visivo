dashboards:
  - name: Chart Examples
    rows:
      - height: small
        items:
          - markdown: |
              # Chart Examples

              This dashboard contains example on some common chart types.

      - height: medium
        items:
          - markdown: |
              # Bar
          - chart:
              traces: # Names for nested objects should be optional, maybe even forbidden to prevent complicated reference structures
                - model: Echo CSV # I Don't think we need the ref
                  props:
                    type: bar
                    x: 
                      query: x
                    y: 
                      query: y
