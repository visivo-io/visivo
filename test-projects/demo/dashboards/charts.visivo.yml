insights:
  - name: Simple Bar
    props:
      type: bar
      x: ?{${ref(Echo CSV).x}}
      y: ?{${ref(Echo CSV).y}}

dashboards:
  - name: Chart Examples
    rows:
      - height: small
        items:
          - markdown:
              content: |
                # Chart Examples

                This dashboard contains example on some common chart types.

      - height: medium
        items:
          - markdown:
              content: |
                # Bar
          - chart:
              name: Bar Chart
              insights:
                - ${ref(Simple Bar)}
