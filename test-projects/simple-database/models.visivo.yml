models:
  - name: second test table name
    sql: "SELECT * FROM test_table"
  - name: volcano
    table_name: volcano
    args:
      - curl
      - '-s'
      -  https://raw.githubusercontent.com/plotly/datasets/master/volcano.csv
      - '| cat'