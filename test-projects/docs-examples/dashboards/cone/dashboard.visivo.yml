includes:
  - path: "simple.visivo.yml"
  - path: "wind-direction.visivo.yml"

dashboards:
  - name: cone
    level: 1
    tags:
      - "Trace Prop"
    rows:
      - height: compact
        items:
          - markdown:
              content: "# Cone"
          - markdown:
              content: ""
            width: 5
      - height: medium
        items:
          - chart: ${ref(simple-cone-plot)}
          - chart: ${ref(wind-direction-cone-plot)}