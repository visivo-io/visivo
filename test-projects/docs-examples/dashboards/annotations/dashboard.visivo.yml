includes:
  - path: "highlight-region.visivo.yml"
  - path: "multiple-regions.visivo.yml"
  - path: "region-with-annotation.visivo.yml"
  - path: "threshold-line.visivo.yml"
  - path: "vertical-threshold.visivo.yml"
  - path: "threshold-with-annotation.visivo.yml"
  - path: "point-annotations.visivo.yml"
  - path: "emoji-points.visivo.yml"
  - path: "emoji-threshold.visivo.yml"
  - path: "emoji-region.visivo.yml"

dashboards:
  - name: annotation-and-shapes
    description: "Examples of how you may use annotations and shapes to enahnce your charts"
    tags: 
      - "chart feature"
    level: 0

    rows:
      - height: compact
        items:
          - markdown:
              content: "# Shapes and Annotations"
              align: left
      - height: medium
        items:
          - chart: ${ref(highlight-region-example)}
          - chart: ${ref(multiple-regions-example)}
      - height: medium
        items:
          - chart: ${ref(region-with-annotation-example)}
          - chart: ${ref(threshold-line-example)}
      - height: medium
        items:
          - chart: ${ref(vertical-threshold-example)}
          - chart: ${ref(threshold-with-annotation-example)}
      - height: medium
        items:
          - chart: ${ref(point-annotations-example)}
          - chart: ${ref(emoji-points-example)}
      - height: medium
        items:
          - chart: ${ref(emoji-threshold-example)}
          - chart: ${ref(emoji-region-example)}
