includes:
  - path: simple.visivo.yml
  - path: lines.visivo.yml
  - path: custom-markers.visivo.yml

dashboards:
  - name: scattergl-dashboard
    level: 1
    tags:
      - scattergl
      - webgl
      - visualization
    rows:
      - height: compact
        items:
          - markdown:
              content: |
                # Scattergl Dashboard

                This dashboard demonstrates different ways to create high-performance scatter plots using the `scattergl` trace type.
      - height: medium
        items:
          - chart: ${ref(simple-scattergl-chart)}
          - chart: ${ref(scattergl-chart-with-lines)}
      - height: medium
        items:
          - chart: ${ref(scattergl-chart-with-custom-markers)} 